"""Example module demonstrating project structure.

This module is a placeholder. Add new modules in the `src/modules/` directory.
Each module should be self-contained, focusing on a single feature or command.
Shared logic should reside in `src/utils/`.
"""

from ..utils.example_helper import example_helper


def run_example() -> None:
    """Run the example feature using shared utilities."""
    example_helper()
